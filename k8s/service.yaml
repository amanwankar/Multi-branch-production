
apiVersion: v1
kind: Service
metadata:
  name: multibranch-flask-app
spec:
  type: NodePort
  selector:
    app: multibranch-flask-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
