apiVersion: v1
kind: Service

metadata:
  name: e-commerce-service
  namespace: e-commerce-namespace

spec:
  selector:
    app: e-commerce-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: LoadBalancerapiVersion: v1
kind: Service

metadata:
  name: e-commerce-service
  namespace: e-commerce-namespace

spec:
  selector:
    app: e-commerce-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: LoadBalancer